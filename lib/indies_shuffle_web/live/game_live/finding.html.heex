<div class="max-w-4xl mx-auto p-6">
  <div class="bg-white rounded-lg shadow-lg p-8">
    <h2 class="text-3xl font-bold text-center text-gray-800 mb-6">🕵️ Find Your Team</h2>
    
    <%= if @player_info.group_id do %>
      <div class="text-center mb-8">
        <div class="text-8xl mb-4"><%= @player_info.group_emoji %></div>
        <h3 class="text-2xl font-semibold text-gray-700 mb-4">Your Group</h3>
        
        <div class="bg-blue-50 border border-blue-200 rounded-lg p-6 mb-6">
          <h4 class="font-semibold text-lg mb-3">Team Members:</h4>
          <div class="grid grid-cols-1 md:grid-cols-2 gap-3">
            <%= for member <- @player_info.group_members do %>
              <div class="flex items-center justify-between bg-white rounded-lg p-3 shadow-sm">
                <span class="font-medium"><%= member.name %></span>
                <%= if member.indie_id == @player_info.player_id do %>
                  <span class="text-blue-600 font-semibold">(You)</span>
                <% end %>
                <%= if @player_info.is_leader and member.indie_id == @player_info.player_id do %>
                  <span class="text-yellow-600 font-semibold">👑 Leader</span>
                <% else %>
                  <%= if member.indie_id == (@player_info.group_members |> Enum.find(&(&1.indie_id == @player_info.leader_id), %{}) |> Map.get(:indie_id)) do %>
                    <span class="text-yellow-600 font-semibold">👑</span>
                  <% end %>
                <% end %>
              </div>
            <% end %>
          </div>
        </div>
        
        <%= if @player_info.is_leader do %>
          <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-4 mb-4">
            <p class="text-yellow-800 font-semibold">
              👑 You are the team leader! You'll be responsible for submitting the final answer.
            </p>
          </div>
        <% end %>
        
        <div class="bg-gray-50 border border-gray-200 rounded-lg p-4">
          <p class="text-gray-700">
            Get to know your teammates! In the next phase, you'll work together to solve a puzzle.
            Each team member will have different clues, so communication is key!
          </p>
        </div>
      </div>
    <% else %>
      <div class="text-center">
        <div class="text-6xl mb-4">🔄</div>
        <p class="text-lg text-gray-600">
          Assigning you to a group...
        </p>
      </div>
    <% end %>
  </div>
</div>