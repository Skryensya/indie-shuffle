<div class="max-w-4xl mx-auto p-6">
  <div class=" rounded-lg shadow-lg p-8">
    <h2 class="text-3xl font-bold text-center text-gray-800 mb-6">🕵️ Find Your Team</h2>
    
    <%= if @player_info.group_id do %>
      <div class="text-center mb-8">
        <div class="text-8xl mb-4"><%= @player_info.group_emoji %></div>
        <h3 class="text-2xl font-semibold text-gray-700 mb-4">Your Group</h3>
        
        <div class="bg-blue-50 border border-blue-200 rounded-lg p-6 mb-6">
          <h4 class="font-semibold text-lg mb-3">Team Members:</h4>
          <div class="grid grid-cols-1 md:grid-cols-2 gap-3">
            <%= for member <- @player_info.group_members do %>
              <div class=" rounded-lg p-3 shadow-sm">
                <div class="flex items-center justify-between mb-2">
                  <span class="font-medium"><%= member.name %></span>
                  <%= if member.indie_id == @player_info.player_id do %>
                    <span class="text-blue-600 font-semibold text-sm">(Tú)</span>
                  <% end %>
                </div>
                <div class="flex items-center gap-2 text-sm">
                  <%= case Map.get(member, :role) do %>
                    <% :decoder -> %>
                      <span class="bg-yellow-100 text-yellow-800 px-2 py-1 rounded font-semibold">
                        🔑 Descifrador
                      </span>
                    <% :code_holder -> %>
                      <span class="bg-purple-100 text-purple-800 px-2 py-1 rounded font-semibold">
                        📜 Guardián del Código
                      </span>
                    <% :solver -> %>
                      <span class="bg-green-100 text-green-800 px-2 py-1 rounded font-semibold">
                        🧩 Solucionador
                      </span>
                    <% _ -> %>
                      <span class="bg-gray-100 text-gray-800 px-2 py-1 rounded">
                        Jugador
                      </span>
                  <% end %>
                </div>
              </div>
            <% end %>
          </div>
        </div>
        
        <%= if @player_info.is_leader do %>
          <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-4 mb-4">
            <p class="text-yellow-800 font-semibold">
              🔑 ¡Eres el Descifrador! Serás el único que puede enviar la respuesta final una vez que tu equipo haya resuelto el puzzle.
            </p>
          </div>
        <% end %>

        <div class="bg-gray-50 border border-gray-200 rounded-lg p-4">
          <h4 class="font-semibold text-gray-800 mb-2">🎯 Cómo jugar:</h4>
          <ul class="text-gray-700 space-y-2 text-left">
            <li><strong>🧩 Solucionadores:</strong> Tienen pistas para resolver el puzzle</li>
            <li><strong>📜 Guardián del Código:</strong> Tiene información especial sobre el código secreto</li>
            <li><strong>🔑 Descifrador:</strong> Es el único que puede enviar la respuesta final</li>
          </ul>
          <p class="text-gray-700 mt-3">
            ¡Conozcan a sus compañeros de equipo! En la siguiente fase, trabajarán juntos para resolver el puzzle.
            Cada miembro tiene información diferente, ¡así que la comunicación es clave!
          </p>
        </div>
      </div>
    <% else %>
      <div class="text-center">
        <div class="text-6xl mb-4">🔄</div>
        <p class="text-lg text-gray-600">
          Assigning you to a group...
        </p>
      </div>
    <% end %>
  </div>
</div>